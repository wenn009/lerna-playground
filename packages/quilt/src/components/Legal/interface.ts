import { StyledCss } from '../../theme/structures';
import { AnyProp } from '../QuiltComponent';

type LegalProps = AnyProp;

type LegalTheme = {
  text?: StyledCss;
};

export { LegalProps, LegalTheme };
