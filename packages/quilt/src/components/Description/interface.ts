import { StyledCss } from '../../theme/structures';
import { AnyProp } from '../QuiltComponent';

type DescriptionProps = AnyProp;

type DescriptionTheme = {
  text?: StyledCss;
};

export { DescriptionProps, DescriptionTheme };
