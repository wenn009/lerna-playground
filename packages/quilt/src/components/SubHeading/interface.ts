import { StyledCss } from '../../theme/structures';
import { AnyProp } from '../QuiltComponent';

type SubHeadingProps = AnyProp;

type SubHeadingTheme = {
  text?: StyledCss;
};

export { SubHeadingProps, SubHeadingTheme };
